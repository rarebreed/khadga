/**
 * This is sort of a FP-ish way of doing Page Object Model.  POM is sort of like dependency
 * injection where you disentangle action on an element from the element.  The most simple form of
 * DI is to simply pass in the formerly hard-coded data.  That's basically what we are going to do
 * here
 */

/**
 * Retrieves the 
 */
export const getSigninButton = (selector: string = "") => {
  return selector
}